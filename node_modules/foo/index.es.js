const alpha = ()=>{ console.log(`alpha`); };
const beta = 'beta';
const speak = (c) => { console.log(`color: ${c}`); };
const api = {
  alpha: alpha,
  beta: beta,
  speak: speak
};

export { alpha, beta, speak };
export default api;
